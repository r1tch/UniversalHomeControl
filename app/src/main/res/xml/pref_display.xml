<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <CheckBoxPreference
        android:key="pref_wakeup"
        android:title="@string/pref_wakeup"
        android:summary="@string/pref_wakeup_summ"
        android:defaultValue="true" />

    <EditTextPreference
        android:inputType="number"
        android:defaultValue=""
        android:key="pref_wakeup_zone"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_title_wakeup_zone" />

    <!-- TODO add pref for: grouping by rooms; ...? -->
</PreferenceScreen>